# myth Wave 7 — R1-A negative fixtures (20 near-miss cases)
#
# Rule: rm -rf flag permutation on dangerous paths (Bedrock, Level 5).
# Each case is a near-miss: structurally similar to R1-A but must NOT match
# R1-A nor any other rule (FP=0 invariant).

rule_context: R1-A
expected: no_match
cases:
  - id: "01-rm_rf_tmp_scratch"
    input: "rm -rf /tmp/scratch"
    note: "/tmp is sandbox-safe"
  - id: "02-rm_rf_tmp_bare"
    input: "rm -rf /tmp"
    note: "/tmp is sandbox-safe"
  - id: "03-rm_rf_relative_build"
    input: "rm -rf ./build"
    note: "relative path"
  - id: "04-rm_rf_build_output"
    input: "rm -rf build/output"
    note: "relative subdirectory"
  - id: "05-rm_f_single_flag"
    input: "rm -f /tmp/foo"
    note: "only -f, no recursive"
  - id: "06-rm_no_flags"
    input: "rm /tmp/file.txt"
    note: "no flags"
  - id: "07-rm_r_only"
    input: "rm -r /tmp/subdir"
    note: "-r without -f (interactive prompts)"
  - id: "08-rm_rf_target_debug"
    input: "rm -rf target/debug"
    note: "cargo build artifact"
  - id: "09-rm_rf_node_modules"
    input: "rm -rf node_modules"
    note: "npm deps cleanup"
  - id: "10-rm_rf_dot_target"
    input: "rm -rf ./target"
    note: "dotted relative"
  - id: "11-rm_i_interactive"
    input: "rm -i /tmp/x"
    note: "interactive mode"
  - id: "12-rmdir_not_rm"
    input: "rmdir /bin/emptydir"
    note: "rmdir is not rm"
  - id: "13-mv_not_rm"
    input: "mv /bin/foo /tmp/"
    note: "different verb"
  - id: "14-rm_rf_build_dir_env"
    input: 'rm -rf "${BUILD_DIR}"'
    note: "non-sentinel env var"
  - id: "15-rm_rf_output_path_env"
    input: 'rm -rf "$OUTPUT_PATH"'
    note: "non-sentinel env var"
  - id: "16-rm_rf_tmp_project"
    input: "rm -rf /tmp/project-abc123"
    note: "session scratch dir"
  - id: "17-rm_rf_dev_shm"
    input: "rm -rf /dev/shm/session"
    note: "tmpfs session dir"
  - id: "18-rm_rf_dot_cache"
    input: 'rm -rf "./.cache"'
    note: "relative cache dir"
  - id: "19-rm_r_f_split_tmp"
    input: "rm -r -f /tmp/x"
    note: "split flags on safe path"
  - id: "20-rm_relative_file"
    input: "rm ./output.log"
    note: "plain rm relative"
