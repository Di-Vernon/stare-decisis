# myth Wave 7 — R1-G negative fixtures (18 near-miss cases)
# Rule: env-path sentinel (rm -rf $HOME / $PROJECT_ROOT / $PWD / ...).

rule_context: R1-G
expected: no_match
cases:
  - id: "01-rm_rf_build_dir_env"
    input: "rm -rf $BUILD_DIR"
    note: "non-sentinel env var"
  - id: "02-rm_rf_tmpdir_brace"
    input: "rm -rf ${TMPDIR}"
    note: "TMPDIR (non-sentinel)"
  - id: "03-rm_rf_output_dir_env"
    input: "rm -rf $OUTPUT_DIR"
    note: "OUTPUT_DIR (non-sentinel)"
  - id: "04-rm_rf_cache_dir_env"
    input: "rm -rf $CACHE_DIR"
    note: "CACHE_DIR (non-sentinel)"
  - id: "05-rm_rf_release_path"
    input: "rm -rf $RELEASE_PATH"
    note: "RELEASE_PATH (non-sentinel)"
  - id: "06-echo_home"
    input: "echo $HOME"
    note: "echo (read-only)"
  - id: "07-ls_home"
    input: "ls $HOME"
    note: "ls (read-only)"
  - id: "08-cd_home"
    input: "cd $HOME"
    note: "cd (no filesystem change)"
  - id: "09-cat_home_bashrc"
    input: "cat $HOME/.bashrc"
    note: "cat (read-only)"
  - id: "10-printenv_home"
    input: "printenv HOME"
    note: "printenv (read-only)"
  - id: "11-export_path"
    input: "export PATH=$PATH:/bin"
    note: "export (env assignment)"
  - id: "12-rm_rf_relative_subdir"
    input: "rm -rf ./subdir"
    note: "relative path (no env sentinel)"
  - id: "13-rm_rf_tmp_absolute"
    input: "rm -rf /tmp"
    note: "absolute tmp (no env sentinel)"
  - id: "14-shutil_rmtree_var"
    input: "shutil.rmtree(some_dir)"
    note: "variable (no env sentinel)"
  - id: "15-fs_rmsync_var"
    input: "fs.rmSync(tmpDir)"
    note: "variable (no env sentinel)"
  - id: "16-mv_not_rm"
    input: "mv $HOME/file /tmp/"
    note: "mv is not rm"
  - id: "17-cp_not_rm"
    input: "cp $HOME/src /tmp/"
    note: "cp is not rm"
  - id: "18-du_not_rm"
    input: "du -sh $HOME"
    note: "du (read-only)"
