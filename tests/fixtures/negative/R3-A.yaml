# myth Wave 7 — R3-A negative fixtures (20 near-miss cases)
# Rule: TLS/JWT verify bypass.

rule_context: R3-A
expected: no_match
cases:
  - id: "01-verify_true"
    input: "verify=True"
    note: "verify=True (opposite)"
  - id: "02-verify_cert_file"
    input: "verify = cert_file"
    note: "verify = variable"
  - id: "03-ssl_verify_env_one"
    input: "SSL_VERIFY=1"
    note: "SSL_VERIFY=1 (truthy)"
  - id: "04-ssl_verify_env_true_lower"
    input: "SSL_VERIFY=true"
    note: "SSL_VERIFY=true (truthy)"
  - id: "05-insecure_skip_verify_false"
    input: "insecure_skip_verify: false"
    note: "insecure_skip_verify: false (safe)"
  - id: "06-insecure_skip_verify_pascal_false"
    input: "InsecureSkipVerify: false"
    note: "InsecureSkipVerify: false (safe)"
  - id: "07-verify_mode_cert_required"
    input: "VerifyMode = CERT_REQUIRED"
    note: "VerifyMode required (safe)"
  - id: "08-ssl_create_default"
    input: "ctx = ssl._create_default_context()"
    note: "default context (verified)"
  - id: "09-session_verify_cert_path"
    input: "session.verify = '/path/to/cert.pem'"
    note: "verify = cert path string"
  - id: "10-requests_verify_cert_path"
    input: "requests.get(url, verify='/etc/ssl/cert.pem')"
    note: "requests verify=cert path"
  - id: "11-jwt_verify_no_option"
    input: "jwt.verify(token, key)"
    note: "jwt.verify without options"
  - id: "12-jwt_verify_opt_true"
    input: "jwt.verify(token, key, { verify: true })"
    note: "jwt with verify:true"
  - id: "13-comment_about_verify"
    input: "// verify flag enabled in production"
    note: "comment without =False"
  - id: "14-verify_signature_function"
    input: "verify_signature(data, sig)"
    note: "verify_signature call (not verify=)"
  - id: "15-verify_function_call"
    input: "verify(password)"
    note: "verify() call"
  - id: "16-verifying_key_class"
    input: "VerifyingKey(key)"
    note: "VerifyingKey class"
  - id: "17-verify_mode_required_python"
    input: "verify_mode = ssl.CERT_REQUIRED"
    note: "lowercase verify_mode required (safe)"
  - id: "18-insecure_skip_verify_camel"
    input: "insecureSkipVerify: false"
    note: "camelCase (different form)"
  - id: "19-if_verify_conditional"
    input: "if verify: proceed()"
    note: "verify as conditional"
  - id: "20-verify_ssl_env"
    input: "VERIFY_SSL=True"
    note: "different env name"
