# myth Wave 7 — R1-G positive fixtures (18 cases)
# Rule: env-path sentinel (rm -rf $HOME / $PROJECT_ROOT / $PWD / $CLAUDE_PROJECT_DIR).

rule: R1-G
expected_level: 5
category: bedrock
cases:
  - id: "01-rm_rf_home"
    input: "rm -rf $HOME"
    note: "bare $HOME"
  - id: "02-rm_rf_brace_home"
    input: "rm -rf ${HOME}"
    note: "braced ${HOME}"
  - id: "03-rm_rf_pwd"
    input: "rm -rf $PWD"
    note: "bare $PWD"
  - id: "04-rm_rf_brace_pwd"
    input: "rm -rf ${PWD}"
    note: "braced ${PWD}"
  - id: "05-rm_rf_project_root"
    input: "rm -rf $PROJECT_ROOT"
    note: "bare $PROJECT_ROOT"
  - id: "06-rm_rf_brace_project_root"
    input: "rm -rf ${PROJECT_ROOT}"
    note: "braced ${PROJECT_ROOT}"
  - id: "07-rm_rf_claude_project_dir"
    input: "rm -rf $CLAUDE_PROJECT_DIR"
    note: "Claude Code project dir"
  - id: "08-rm_rf_brace_claude_project_dir"
    input: "rm -rf ${CLAUDE_PROJECT_DIR}"
    note: "braced CLAUDE_PROJECT_DIR"
  - id: "09-rm_rf_oldpwd"
    input: "rm -rf $OLDPWD"
    note: "bare $OLDPWD"
  - id: "10-rm_rf_brace_oldpwd"
    input: "rm -rf ${OLDPWD}"
    note: "braced ${OLDPWD}"
  - id: "11-rm_Rf_home"
    input: "rm -Rf $HOME"
    note: "uppercase R flag"
  - id: "12-rm_fR_pwd"
    input: "rm -fR $PWD"
    note: "fR flag order"
  - id: "13-rm_fr_brace_home"
    input: "rm -fr ${HOME}"
    note: "fr flag order, braced"
  - id: "14-shutil_rmtree_home"
    input: "shutil.rmtree($HOME)"
    note: "python rmtree with env"
  - id: "15-shutil_rmtree_quoted_home"
    input: "shutil.rmtree('$HOME')"
    note: "python rmtree quoted env literal"
  - id: "16-fs_rmsync_project_root"
    input: "fs.rmSync($PROJECT_ROOT)"
    note: "node fs.rmSync with env"
  - id: "17-fs_rmsync_brace_claude"
    input: "fs.rmSync('${CLAUDE_PROJECT_DIR}')"
    note: "node fs.rmSync braced env quoted"
  - id: "18-rm_rf_quoted_home"
    input: 'rm -rf "$HOME"'
    note: "double-quoted $HOME"
